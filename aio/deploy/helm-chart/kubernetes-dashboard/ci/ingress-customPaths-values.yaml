ingress:
  enabled: true

  customPaths:
    - backend:
        service:
          name: ssl-redirect
          port:
            name: use-annotation
    - backend:
        service:
          name: >-
            {{ include "kubernetes-dashboard.fullname" . }}
          port:
            number: 443
